{
  "name": "firestore-algolia-search",
  "version": "0.0.1",
  "description": "",
  "license": "Apache-2.0",
  "scripts": {
    "emulator:ext:start": "firebase ext:dev:emulators:start --import=data --test-params=test-params.env"
  },
  "devDependencies": {
    "@commitlint/cli": "^12.0.1",
    "@commitlint/config-conventional": "^12.0.1",
    "husky": "^5.1.3"
  }
}
